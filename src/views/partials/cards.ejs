<% productos.forEach(prod => { %>
    <div class="cards">
        <p class="prod-name"><%= prod.name %></p>
        <img src="<%= prod.img %>" alt="<%= prod.name %>">
        <p class="prod-price">$<%= prod.price %></p>
        <a href="/productos/<%= prod.brand %>/<%= prod._id %>" class="btn-carrito">Ver Producto</a>
    </div>
<% }) %>